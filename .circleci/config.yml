version: 2.1
orbs:
  cypress: cypress-io/cypress@1.29.0
  win: circleci/windows@2.2.0

  jobs:
    platform: # name of your job
    executor:
      name: win/default # executor type
      size: "medium"
executors:
  with-chrome:
    docker:
      - image: 'cypress/browsers:node14.16.0-chrome90-ff88'
workflows:
  build:
    jobs:
      - cypress/install
      - cypress/run:
          executor: with-chrome
          browser: chrome
          name: Chrome
          requires:
            - cypress/install
          post-steps:
            - run: npm run testcase2
      - cypress/run:
          name: Firefox
          requires:
            - cypress/install
          executor: with-chrome
          start: npm start
          browser: firefox
          post-steps:
            - run: npm run testcase2
